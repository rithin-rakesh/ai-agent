[pytest]
markers =
    live_browser: Mark tests that interact with live browser CDP session (skipped in standard CI/unit test runs).
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
